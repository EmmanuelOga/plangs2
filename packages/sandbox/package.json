{
  "name": "@plangs/sandbox",
  "description": "A package to test things during development",
  "version": "0.1.0",
  "devDependencies": {
    "@types/preact-custom-element": "^4.0.4"
  },
  "dependencies": {
    "preact": "^10.22.1",
    "preact-custom-element": "^4.3.0",
    "@plangs/frontend": "workspace:*"
  },
  "scripts": {
    "dev": "esbuild src/sandbox.tsx --bundle --sourcemap --outdir=public --watch"
  }
}
