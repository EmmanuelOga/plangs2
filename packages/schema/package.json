{
  "name": "@plangs/schema",
  "version": "0.0.0",
  "type": "module",
  "description": "Single source of truth for the Plangs graph: node kinds, edges, Zod schemas, inference rules.",
  "exports": {
    ".": "./src/index.ts"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "catalog:"
  },
  "devDependencies": {
    "typescript": "catalog:"
  }
}
