import type { PlangsGraph } from "@plangs/plangs";

export function define(g: PlangsGraph) {
  g.nodes.pl.set("pl+adblock-filter-list", { name: "Adblock Filter List", languishRanking: 50 });

  // TOOLS

  // TOOL BUNDLES

  // LIBRARIES

  // APPS
}
