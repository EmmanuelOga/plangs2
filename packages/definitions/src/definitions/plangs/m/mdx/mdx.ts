import type { PlangsGraph } from "@plangs/plangs";

export function define(g: PlangsGraph) {
  g.nodes.pl.set("pl+mdx", {
    name: "MDX",
    languishRanking: 66,
    githubName: "MDX",
    githubLangId: "512838272",
    githubColor: "#fcb32c",
    githubPopular: false,
    githubType: "markup",
  });

  // TOOLS

  // TOOL BUNDLES

  // LIBRARIES

  // APPS
}
