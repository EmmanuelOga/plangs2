import type { PlangsGraph } from "@plangs/plangs";

export function define(g: PlangsGraph) {
<% for (const bundle of it.data) { %>
  g.<%= it.nodeType %>.define(<%= bundle.vid %>)<%= bundle.data ? `.merge(${bundle.data})` : '' %><%= bundle.vrelations?.join('') ?? '' %>;

  /**/

<% } %>
}
