# pnpm is provisioned by mise (10.34.5); don't let pnpm self-switch versions.
manage-package-manager-versions=false
# Keep node_modules strict (no phantom deps) — the new packages are ESM-clean.
link-workspace-packages=true
