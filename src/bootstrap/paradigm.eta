import type { PlangsGraph } from '../../entities/plangs_graph';

export function define(g: PlangsGraph) {
   const lb = g.paradigmBuilder;

<% for (const [lid, name, websites] of it.paradigms) { %>
  lb.define(
    <%= lid %>,
    <%= name %>,
    <%= websites %>);

<% } %>
}
