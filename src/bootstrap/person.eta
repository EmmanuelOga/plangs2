import type { PlangsGraph } from "../../../entities/plangs_graph";

export function define(g: PlangsGraph) {
   const lb = g.personBuilder;

<% for (const [pvid, name, websites] of it.people) { %>
  lb.define(
    <%= pvid %>,
    <%= name %>,
    <%= websites %>);

<% } %>
}