import type { PlangsGraph } from '../../entities/plangs_graph';

export function define(g: PlangsGraph) {
   const lb = g.<%= it.builderName %>Builder;

<% for (const [lid, name, values] of it.data) { %>
  //------------------------------------------------------------------------------- 

  lb.define(
    <%= lid %>,
    <%= name %>,
    <%= values.data %><% if (values.vrelations) { %>,
    <%= values.vrelations %>
    <% } %>);

<% } %>
}
