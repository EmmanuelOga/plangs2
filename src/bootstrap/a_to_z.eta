import type { PlangsGraph } from '<%= '../'.repeat(it.depth) %>entities/plangs_graph';

export function define(g: PlangsGraph) {
<% for (const bundle of it.data) { %>
  g.<%= it.builderName %>(<%= bundle.vid %>)
  <% if (bundle.data) { %> .merge(<%= bundle.data %>) <% } %>;

<% } %>
}
