import { Wade } from "../../vendor/Wade/Wade";

type SearchIndex = {
  search: (term: string) => string[];
};

function searchIndex(values: [string, string]) {
  //   const plIdx = Wade([...pg.v_plang].map(([vid, pl]) => pl.name));
}
